"# MTech Solutions - Deployment Guide\\## Deployment Options\\### 1. VPS/Cloud Server (Ubuntu/Debian)\\**Backend Setup:\```bash\# Install Node.js \\(npm, nginx, certbot\\)\sudo apt update\ curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash - sudo apt-get install -y nodejs\\# Upload & install dependencies\ cd /var/www/mtech/server\ npm install --production\\# Setup MySQL\ sudo mysql -u root -p < schema.sql\\# Run with PM2\ sudo npm install -g pm2\ pm2 start server/index.js --name mtech-api\ pm2 startup\\# Nginx reverse proxy\ server {\   listen 80;\   server_name api.mtechsolutions.com;\   location / {\     proxy_pass http://localhost:5000;\     proxy_set_header Host $host;\   }\ }\```\\**Frontend Build & Serve:\```bash\cd client\ npm run build\# Copy dist to web root\ cp -r dist/* /var/www/html\```\\---\\### 2. Hostinger Shared Hosting\\- Upload built React app to `public_html`\- Place Node.js backend in a subdirectory or use their Node.js hosting\- Configure `.env` with production DB credentials\- Ensure CORS allows your domain\\---\\### 3. AWS EC2/Elastic Beanstalk\\- Create EC2 instance with Node.js\- Set up RDS for MySQL\- Deploy with Elastic Beanstalk CLI or use Docker\- Configure security groups for ports 80/443\- Use application load balancer for scaling\\---\\### 4. DigitalOcean App Platform\\- Create new app from GitHub\- Add environment variables in dashboard\- Select Node.js environment\- Add MySQL database cluster\- Deploy automatically on push\\---\\## Environment Variables Required\\| Variable | Description |\|----------|-------------|\| `DB_HOST` | MySQL host address |\| `DB_USER` | MySQL username |\| `DB_PASSWORD` | MySQL password |\| `DB_NAME` | Database name |\| `JWT_SECRET` | Secret for signing JWTs |\| `PORT` | Server port |\| `CLIENT_URL` | Frontend URL for CORS |\| `OPENAI_API_KEY` | For AI assistant \(optional\\) |\\---\\## SSL/TLS Setup\\Use Let\\'s Encrypt for HTTPS:\```bash\sudo certbot --nginx -d mtechsolutions.com\sudo certbot --nginx -d api.mtechsolutions.com\```"