"# MTech Solutions - Deployment Guide\\n\\n## Deployment Options\\n\\n### 1. VPS/Cloud Server (Ubuntu/Debian)\\n\\n**Backend Setup:\\n```bash\\n# Install Node.js \\(npm, nginx, certbot\\)\\nsudo apt update\\n curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash - sudo apt-get install -y nodejs\\n\\n# Upload & install dependencies\\n cd /var/www/mtech/server\\n npm install --production\\n\\n# Setup MySQL\\n sudo mysql -u root -p < schema.sql\\n\\n# Run with PM2\\n sudo npm install -g pm2\\n pm2 start server/index.js --name mtech-api\\n pm2 startup\\n\\n# Nginx reverse proxy\\n server {\\n   listen 80;\\n   server_name api.mtechsolutions.com;\\n   location / {\\n     proxy_pass http://localhost:5000;\\n     proxy_set_header Host $host;\\n   }\\n }\\n```\\n\\n**Frontend Build & Serve:\\n```bash\\ncd client\\n npm run build\\n# Copy dist to web root\\n cp -r dist/* /var/www/html\\n```\\n\\n---\\n\\n### 2. Hostinger Shared Hosting\\n\\n- Upload built React app to `public_html`\\n- Place Node.js backend in a subdirectory or use their Node.js hosting\\n- Configure `.env` with production DB credentials\\n- Ensure CORS allows your domain\\n\\n---\\n\\n### 3. AWS EC2/Elastic Beanstalk\\n\\n- Create EC2 instance with Node.js\\n- Set up RDS for MySQL\\n- Deploy with Elastic Beanstalk CLI or use Docker\\n- Configure security groups for ports 80/443\\n- Use application load balancer for scaling\\n\\n---\\n\\n### 4. DigitalOcean App Platform\\n\\n- Create new app from GitHub\\n- Add environment variables in dashboard\\n- Select Node.js environment\\n- Add MySQL database cluster\\n- Deploy automatically on push\\n\\n---\\n\\n## Environment Variables Required\\n\\n| Variable | Description |\\n|----------|-------------|\\n| `DB_HOST` | MySQL host address |\\n| `DB_USER` | MySQL username |\\n| `DB_PASSWORD` | MySQL password |\\n| `DB_NAME` | Database name |\\n| `JWT_SECRET` | Secret for signing JWTs |\\n| `PORT` | Server port |\\n| `CLIENT_URL` | Frontend URL for CORS |\\n| `OPENAI_API_KEY` | For AI assistant \(optional\\) |\\n\\n---\\n\\n## SSL/TLS Setup\\n\\nUse Let\\'s Encrypt for HTTPS:\\n```bash\\nsudo certbot --nginx -d mtechsolutions.com\\nsudo certbot --nginx -d api.mtechsolutions.com\\n```"